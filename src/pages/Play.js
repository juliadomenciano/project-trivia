import React, { Component } from 'react';
import Header from '../components/Header';

class Play extends Component {
  render() {
    return (
      <section>
        <Header />
      </section>
    );
  }
}

export default Play;
